package component:wcmarkdown2pdf@1.0.0;

world conversion {
  export generate: func(input: string) -> result<list<u8>, string>;
  export generatetofile: func(input: string, filepath: string) -> result<_, string>;
}
